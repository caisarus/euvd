# CRA Article 14 — Early Warning DRAFT

> {{ disclaimer }}

Generated by euvd-watch at {{ generated_at }} (all times UTC).

## 1. Reporter (from your euvd-watch configuration)

- **Manufacturer / organization:** {{ organization.name }}
- **Contact e-mail:** {{ organization.contact_email }}
- **Product:** {{ organization.product_name }}
- **Contact person for follow-up:** TODO-HUMAN (name and availability)

## 2. Vulnerability identification

- **EUVD id:** {{ vulnerability.euvd_id }}
{% if vulnerability.cves %}
- **CVE:** {{ vulnerability.cves | join(", ") }}
{% else %}
- **CVE:** none published in EUVD at drafting time
{% endif %}
{% if vulnerability.cvss_score is not none %}
- **CVSS (as published by EUVD):** {{ vulnerability.cvss_score }}
{% endif %}
- **Description (from EUVD):** {{ vulnerability.description or "TODO-HUMAN (EUVD published no description)" }}

## 3. Affected product / component

- **Component:** {{ component.name }}{{ " " ~ component.version if component.version else "" }}
{% if component.purl %}
- **Package identifier (purl):** `{{ component.purl }}`
{% endif %}
- **How it was matched:** {{ match_explanation }} (confidence: {{ match_confidence }})
- **Where this component is used in {{ organization.product_name }}:** TODO-HUMAN
- **Impact assessment (severity, affected users, cross-border reach):** TODO-HUMAN

## 4. Basis of awareness (why the reporting trigger fired)

euvd-watch first recorded this event at **{{ event.first_seen }}** (event id
`{{ event.event_id }}`). The configured trigger fired on:

{% for sentence in awareness_basis %}
- {{ sentence }}
{% endfor %}

- **Is the vulnerability being exploited in *your* product/deployment specifically?**
  TODO-HUMAN — the signals above concern the component in general, not your product.

{% if vulnerability.references %}
## 5. References (from EUVD)

{% for reference in vulnerability.references %}
- {{ reference }}
{% endfor %}
{% endif %}

## 6. Timeline and deadlines (from your configured stages)

| Stage | Window | Deadline (UTC) | Status at drafting |
|---|---|---|---|
{% for entry in timeline %}
| {{ entry.stage }} | {{ entry.hours }} h | {{ entry.deadline or "awaiting anchor" }} | {{ entry.state }} |
{% endfor %}

## 7. Corrective / mitigating measures

- **Measures taken or planned:** TODO-HUMAN
- **Availability of a fix or workaround for users:** TODO-HUMAN

---

Before filing: verify the currently required notification fields against ENISA/CSIRT
guidance and complete every TODO-HUMAN item.

Where this goes, per Article 14(1): simultaneously to **the CSIRT designated as
coordinator** and to **ENISA**, via the **single reporting platform established pursuant
to Article 16** — not to either recipient alone, and not by ad-hoc e-mail. Confirm the
current platform endpoint and your designated coordinating CSIRT before submitting.

euvd-watch has NOT submitted anything and never will.
